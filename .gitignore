__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.mypy_cache/
_temp/
storm-pulse.wiki/
.env
venv/
# Release-side signing keys (authoring keygen output). The private .key is a
# secret and must NEVER land in this public repo; keep it in your key store.
*.key
storm-release.pub
__pycache__/
*.pyc

# Test Garage state (docker/docker-compose.test.yml bind mounts)
docker/garage-meta/
docker/garage-data/
